rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // FULLY OPEN RULES FOR DEVELOPMENT
    // Security and Data Isolation are managed by the application logic
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
