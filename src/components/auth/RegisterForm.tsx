"use client";

// The registration form is deprecated in the single-organization setup.
// New users are created via the "Invite User" dialog in the settings.
// This component returns null to avoid breaking any imports.
export function RegisterForm() {
  return null;
}
